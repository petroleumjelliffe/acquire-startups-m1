import React,{useState} from 'react';import { Game } from './Game';import { SetupScreen } from './components/SetupScreen';export default function App(){const[config,setConfig]=useState<{seed:string,names:string[]}|null>(null);return(<div className='min-h-screen bg-gray-50 p-6'>{!config?(<SetupScreen defaultSeed='saffold-seed' defaultPlayers='Ava,Ben' onStart={(seed,names)=>setConfig({seed,names})}/>):(<Game seed={config.seed} playerNames={config.names}/>)}</div>);}