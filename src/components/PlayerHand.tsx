import React from 'react';import { Coord } from '../utils/gameHelpers';export function PlayerHand({name,hand,onPlace}:{name:string,hand:Coord[],onPlace:(c:Coord)=>void}){return(<div><h3 className='font-semibold'>{name}'s Hand</h3><div className='flex flex-wrap gap-2 mt-1'>{hand.map(h=>(<button key={h} onClick={()=>onPlace(h)} className='border rounded px-3 py-1 hover:bg-gray-50'>{h}</button>))}</div></div>);}