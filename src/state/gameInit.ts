import type { GameState, Player, TileCell } from './gameTypes';import { generateAllCoords, shuffleSeeded, Coord } from '../utils/gameHelpers';export function createEmptyBoard():Record<Coord,TileCell>{const b:Record<string,TileCell>={};for(const c of generateAllCoords())b[c]={placed:false};return b as Record<Coord,TileCell>;}export function createInitialGame(seed:string,names:string[]):GameState{const bag=shuffleSeeded(generateAllCoords(),seed);const board=createEmptyBoard();const players:Player[]=names.map((n,i)=>({id:`p${i+1}`,name:n,cash:6000,hand:[]}));return{seed,stage:'draw',players,turnIndex:0,board,bag,startups:{},log:[]};}